import 'package:flutter/material.dart';

const kColorPrimary = Color(0xFF149a68);
const kColorPrimaryLight = Color(0xFF6ef4cf);
const kColorGrayLight = Color(0xFFcccecd);
const kColorPrimaryTransparent = Color(0x6C149a68);
const kColorPrimaryDark = Color(0xFF038968);
const kAppName = "GREEN CATALOG";
const kWhiteTransparent=Color(0xC6E0E0E0);

const kBaseUrl='https://api.greencatalogs.ir/';

const kTabTitle = TextStyle(
    fontSize: 14,
    fontFamily: 'sans',
    color: kWhiteTransparent,
    fontWeight: FontWeight.w400
);

const kFollwoerStyle = TextStyle(
    fontSize: 12,
    color: kWhiteTransparent,
    fontFamily: 'sansBold'
);

const kFollwoerStyleNumber = TextStyle(
    fontSize: 16,
    color: kWhiteTransparent,
    fontFamily: 'sans'
);

const kTextStyleName = TextStyle(
    fontSize: 16,
    color: kWhiteTransparent,
    fontFamily: 'sansBold'

);

const kTextStyleTitleAlert = TextStyle(
    fontSize: 16,
    color: kColorPrimary,
    fontFamily: 'sansBold'

);

